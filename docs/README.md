# envctl docs

Astro Starlight site. Pages live in `src/content/docs/`.

    npm install
    npm run dev       # http://localhost:4321/docs/
    npm run build     # static site in dist/

`public/agent-guide.md` is a single-file summary an agent can read before helping someone set envctl up.
